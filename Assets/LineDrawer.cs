using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LineDrawer : MonoBehaviour {

	//List<List<Float, Float, Float>> lineData = new List<List<Float, Float, Float>>();
	int frameCounter = 0;
	
	// Update is called once per frame
	void Update () {
		//lineDataForThisFrame = lineData[frameCounter];

		
		
		frameCounter++;
	}
}
